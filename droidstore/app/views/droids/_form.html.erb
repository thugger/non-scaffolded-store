<%= form_for(@droid) do |f| %>
  <% if @droid.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@droid.errors.count, "error") %> prohibited this droid from being saved:</h2>

      <ul>
      <% @droid.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :droid_name %><br>
    <%= f.text_field :droid_name %>
  </div>
  <div class="field">
    <%= f.label :price %><br>
    <%= f.text_field :price %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
